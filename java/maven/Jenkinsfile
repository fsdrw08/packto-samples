pipeline {
    agent none
    stages {
        stage("Build") {
            agent {
                dockerContainer {
                    image "buildpacksio/pack:latest"
                }
            }
            stages {
                stage("Build A") {
                    steps {
                        sh "pack --version"
                    }
                }
            }
        }
    }
}